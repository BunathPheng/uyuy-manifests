---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uyuy                  # Ingress name
  labels:
    app: uyuy
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "uyuy.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uyuy               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
